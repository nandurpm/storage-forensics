# Local visual verification

The synthetic demo report at `http://127.0.0.1:4094` rendered a clear read-only and privacy boundary, inventory metrics, capacity data, folder-size map, extension distribution, largest-file table, duplicate groups, and old-file table. Its synthetic fixture reported four files, one same-content duplicate group, `23 B` potentially reclaimable only after review, and one old file. The report remained explicit that it does not change, delete, move, or upload source files.

A `390 × 844` narrow-viewport capture saved as `notes/screenshots/mobile-390.png` confirmed that the report title, boundary notice, metrics, and tables begin in a readable single-column mobile flow without horizontal clipping.
