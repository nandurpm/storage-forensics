{
  "name": "storage-forensics",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Read-only, local storage analysis with metadata inventory, duplicate grouping, snapshots, and reports.",
  "bin": {
    "storage-forensics": "./src/cli.mjs"
  },
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check src/*.mjs",
    "demo": "node src/cli.mjs demo --out reports/demo",
    "start": "node src/render.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "license": "MIT"
}
